# Data Model: [Feature Name]

<!--
  Feature: [NNN]-[feature-slug]
  Plan reference: plan.md
  Last updated: [DATE]
-->

---

## Status
`Draft` · `In Review` · `Approved`

---

## New Entities

### [EntityName]

| Column | Type | Required | Default | Description |
|---|---|---|---|---|
| `id` | UUID / BIGINT | Yes | auto | Primary key |
| `[field_name]` | [VARCHAR/TEXT/INT/BOOL/TIMESTAMP] | Yes/No | [default] | [What it stores] |
| `created_at` | TIMESTAMP | Yes | now() | Record creation time |
| `updated_at` | TIMESTAMP | Yes | now() | Last modification time |

**Indexes:**
- Primary: `id`
- [Additional index: `field_name` for [reason]]

**Constraints:**
- `[field_name]` UNIQUE / NOT NULL / FK → `[other_table].[id]`

**Notes:**
- [Any important business rules about this entity]

---

## Modified Entities

### [ExistingEntityName]

**Changes:**

| Column | Change Type | Old | New | Reason |
|---|---|---|---|---|
| `[column]` | Added / Modified / Removed | [old] | [new] | [why] |

**Migration notes:**
- [Any data migration required? Backfill strategy?]
- [Rollback plan if migration fails]

---

## Relationships

```
[EntityA] ──1:N──► [EntityB]
     │
     └──M:N──► [EntityC] (via [junction_table])
```

| From | Relationship | To | Via |
|---|---|---|---|
| [EntityA] | Has many | [EntityB] | `entity_b.entity_a_id` |
| [EntityA] | Belongs to | [EntityC] | `entity_a.entity_c_id` |

---

## Migration Script Skeleton

```sql
-- Migration: [NNN]_[feature_slug]_[description]
-- Date: [DATE]
-- Author: [GENERATED BY AI]

-- Up migration
BEGIN;

CREATE TABLE [entity_name] (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    [field_name] [TYPE] NOT NULL,
    created_at TIMESTAMP WITH TIME ZONE NOT NULL DEFAULT NOW(),
    updated_at TIMESTAMP WITH TIME ZONE NOT NULL DEFAULT NOW()
);

CREATE INDEX idx_[entity_name]_[field] ON [entity_name]([field]);

COMMIT;

-- Down migration (rollback)
BEGIN;
DROP TABLE IF EXISTS [entity_name];
COMMIT;
```

---

## Seed / Reference Data

[Any static or default data that needs to be inserted as part of this feature]

```sql
-- Seed data
INSERT INTO [entity_name] (...) VALUES (...);
```
